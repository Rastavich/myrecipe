import React from "react";
import { DetailsView } from "./RecipeDetails.elements";

const RecipeDetails = (recipe) => {
  return "";
  //   if (recipe == null) {
  //     return "";
  //   } else {
  // const { steps } = recipe;
  // console.log(recipe.recipe);
  //   {
  //     recipe.map((recipes) => <DetailsView>{recipes.recipe_name}</DetailsView>);
  //   }
  // return <DetailsView>{recipe.recipe.recipe_name}</DetailsView>;
};

export default RecipeDetails;
